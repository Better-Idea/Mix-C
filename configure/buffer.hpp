#ifndef pack_configure_buffer
#define pack_configure_buffer

// gc filter 队列元素个数（需要是 2^n）
#define xgc_filter_queue_depth          (65536)

#define xgc_free_list_queue_depth       (8)

#define xthread_limit                   (65536)
#endif
